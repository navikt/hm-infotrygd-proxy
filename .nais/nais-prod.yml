apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: hm-infotrygd-proxy
  namespace: teamdigihot
  labels:
    team: teamdigihot
spec:
  image: {{image}}
  secureLogs:
    enabled: true
  accessPolicy:
    outbound:
      external:
        - host: a01dbfl033.adeo.no